{% extends 'base.html.twig' %}

{% block title %}Hello CategoriesController!{% endblock %}

{% block body %}
    <div class="example-wrapper">
        <h1>Etes-vous sur de vouloir supprimer la categorie<i>{{ categorie.titre }}</i></h1>
        {{ form(formulaire) }}
        <a href="{{ path("app_home") }}" class="btn btn-warning">Annuler</a>
    </div>
{% endblock %}
